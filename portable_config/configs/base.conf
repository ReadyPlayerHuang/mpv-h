# Playback behavior
keep-open=yes              # Keep the player window open after playback ends.
sub-auto=fuzzy             # Fuzzy-match external subtitles near the media file.

# Disable mpv's built-in OSC so uosc can provide the UI.
osc=no
osd-bar=no
